{
    "name": "@prms/result-management-ingestor",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "node ../../node_modules/esbuild/bin/esbuild --config=esbuild.config.mjs",
        "zip": "cd dist && zip -r ../dist.zip ."
    },
    "dependencies": {
        "@aws-sdk/client-s3": "^3.654.0"
    },
    "devDependencies": {
        "esbuild": "^0.24.0",
        "typescript": "^5.6.3"
    }
}