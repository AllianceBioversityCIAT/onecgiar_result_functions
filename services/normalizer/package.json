{
  "name": "@prms/normalizer",
  "version": "1.0.2",
  "private": true,
  "main": "dist/handler.cjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "rm -rf dist && npx esbuild handler.mjs --bundle --platform=node --target=node20 --format=cjs --outfile=dist/handler.cjs"
  },
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.899.0",
    "@aws-sdk/client-s3": "^3.614.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
