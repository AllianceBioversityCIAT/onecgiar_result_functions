{
    "name": "@prms/normalizer",
    "version": "1.0.1",
    "type": "module",
    "main": "dist/handler.js",
    "engines": {
        "node": ">=18"
    },
    "scripts": {
        "build": "rm -rf dist && npx esbuild src/handler.ts --bundle --platform=node --target=node20 --outfile=dist/handler.js"
    },
    "devDependencies": {
        "@types/node": "^20.12.12",
        "esbuild": "^0.23.1",
        "typescript": "^5.5.4"
    },
    "dependencies": {
        "@aws-sdk/client-eventbridge": "^3.899.0",
        "@aws-sdk/client-s3": "^3.614.0"
    }
}
